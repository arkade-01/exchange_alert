{
  "name": "exchange_alert",
  "version": "1.0.0",
  "description": "Scans CEX perpetual futures for rising open interest + volume and posts ranked alerts to Telegram",
  "license": "MIT",
  "author": "arkade",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js --loop",
    "scan": "tsx src/index.ts --once",
    "scan:dry": "tsx src/index.ts --once --dry-run",
    "loop": "tsx src/index.ts --loop",
    "typecheck": "tsc --noEmit",
    "test": "rm -f .test.db .test.db-wal .test.db-shm && DB_PATH=.test.db tsx test/modes.mts && DB_PATH=.test.db tsx test/cooldown.mts && DB_PATH=.test.db tsx test/snapshot.mts && DB_PATH=.test.db tsx test/tracking.mts",
    "report": "tsx src/index.ts --report",
    "scan:premove": "MODE=premove tsx src/index.ts --once --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "dotenv": "^16.4.7",
    "p-limit": "^6.2.0",
    "undici": "^8.10.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.13.5",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
