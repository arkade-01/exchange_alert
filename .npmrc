# better-sqlite3 carries a binding.gyp, so npm will run `node-gyp rebuild` and
# demand a C toolchain unless told otherwise. It ships prebuilt binaries for
# every platform we deploy to; no package here needs an install script.
# Set at project level so every builder behaves the same, whatever npm it picks.
ignore-scripts=true
